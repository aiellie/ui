{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "tooltip-icon-button",
  "title": "Tooltip Icon Button",
  "description": "An icon button that carries its name on a tooltip, so a composer row of glyphs still says what each one does without taking a word's width to do it.",
  "dependencies": [
    "radix-ui"
  ],
  "registryDependencies": [
    "https://ui.aiellie.dev/r/button.json",
    "https://ui.aiellie.dev/r/tooltip.json",
    "https://ui.aiellie.dev/r/utils.json"
  ],
  "files": [
    {
      "path": "components/aiellie-ui/tooltip-icon-button.tsx",
      "content": "\"use client\";\n\nimport { type ComponentPropsWithRef, forwardRef } from \"react\";\nimport { Slot } from \"radix-ui\";\n\nimport {\n  Tooltip,\n  TooltipContent,\n  TooltipProvider,\n  TooltipTrigger,\n} from \"@/components/ui/tooltip\";\nimport { Button } from \"@/components/ui/button\";\nimport { cn } from \"@/lib/utils\";\n\nexport type TooltipIconButtonProps = ComponentPropsWithRef<typeof Button> & {\n  tooltip: string;\n  side?: \"top\" | \"bottom\" | \"left\" | \"right\";\n};\n\nexport const TooltipIconButton = forwardRef<\n  HTMLButtonElement,\n  TooltipIconButtonProps\n>(({ children, tooltip, side = \"bottom\", className, ...rest }, ref) => {\n  return (\n    <TooltipProvider>\n      <Tooltip>\n        <TooltipTrigger render={<Button variant=\"ghost\" size=\"icon\" {...rest} className={cn(\n                            \"aui-button-icon size-6 p-1 active:scale-90\",\n                            className,\n                          )} ref={ref} />}><Slot.Slottable>{children}</Slot.Slottable><span className=\"aui-sr-only sr-only\">{tooltip}</span></TooltipTrigger>\n        <TooltipContent side={side}>{tooltip}</TooltipContent>\n      </Tooltip>\n    </TooltipProvider>\n  );\n});\n\nTooltipIconButton.displayName = \"TooltipIconButton\";\n",
      "type": "registry:ui",
      "target": "components/aiellie-ui/tooltip-icon-button.tsx"
    }
  ],
  "type": "registry:ui"
}