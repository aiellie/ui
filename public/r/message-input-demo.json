{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "message-input-demo",
  "title": "Message Input",
  "description": "A message being written and sent, with the control at the end of the field turning from send to stop while an answer comes back.",
  "dependencies": [
    "ai"
  ],
  "registryDependencies": [
    "https://ui.aiellie.dev/r/message-input.json",
    "https://ui.aiellie.dev/r/tooltip.json"
  ],
  "files": [
    {
      "path": "examples/composer/message-input.tsx",
      "content": "\"use client\"\n\nimport * as React from \"react\"\nimport type { ChatStatus } from \"ai\"\n\nimport {\n  MessageInput,\n  MessageInputField,\n  MessageInputSubmit,\n} from \"@/components/aiellie-ui/composer/message-input\"\nimport { TooltipProvider } from \"@/components/ui/tooltip\"\n\n/**\n * The control at the end of the field is whichever one the field is asking\n * for: send once there is something to send, and stop while an answer is\n * coming back.\n */\nexport function MessageInputDemo() {\n  const [status, setStatus] = React.useState<ChatStatus>(\"ready\")\n  const [value, setValue] = React.useState(\"\")\n  const [outcome, setOutcome] = React.useState<string | null>(null)\n\n  React.useEffect(() => {\n    if (status !== \"streaming\") return undefined\n    const id = setTimeout(() => {\n      setStatus(\"ready\")\n      setOutcome(\"Answered.\")\n    }, 4000)\n    return () => clearTimeout(id)\n  }, [status])\n\n  // Derived rather than set from each handler: the note is a reading of the\n  // state, and a note written by whichever handler fired last is a note that\n  // disagrees with what is on screen.\n  const note =\n    status === \"streaming\"\n      ? \"Answering, so the control is stop.\"\n      : value.trim()\n        ? \"Something to send, so send is lit.\"\n        : (outcome ?? \"Nothing to send, so send is dimmed.\")\n\n  return (\n    <TooltipProvider>\n      <div className=\"flex w-full max-w-sm flex-col gap-2\">\n        <MessageInput\n          value={value}\n          onValueChange={(next) => {\n            setValue(next)\n            if (next) setOutcome(null)\n          }}\n          onSubmit={() => setStatus(\"streaming\")}\n        >\n          <MessageInputField placeholder=\"Send a message…\" />\n          <MessageInputSubmit\n            status={status}\n            onStop={() => {\n              setStatus(\"ready\")\n              setOutcome(\"Stopped.\")\n            }}\n          />\n        </MessageInput>\n        <p className=\"ps-1 text-xs text-muted-foreground\">{note}</p>\n      </div>\n    </TooltipProvider>\n  )\n}\n\n/** The three the control can be, side by side rather than one at a time. */\nexport function MessageInputStatesDemo() {\n  return (\n    <TooltipProvider>\n      <div className=\"flex w-full max-w-sm flex-col gap-4\">\n        <div className=\"flex flex-col gap-1.5\">\n          <p className=\"ps-1 text-xs text-muted-foreground\">\n            Empty — send, dimmed\n          </p>\n          <MessageInput value=\"\" onValueChange={() => {}}>\n            <MessageInputField placeholder=\"Send a message…\" />\n            <MessageInputSubmit />\n          </MessageInput>\n        </div>\n        <div className=\"flex flex-col gap-1.5\">\n          <p className=\"ps-1 text-xs text-muted-foreground\">Written — send</p>\n          <MessageInput\n            value=\"Are we still shipping on Tuesday?\"\n            onValueChange={() => {}}\n          >\n            <MessageInputField />\n            <MessageInputSubmit />\n          </MessageInput>\n        </div>\n        <div className=\"flex flex-col gap-1.5\">\n          <p className=\"ps-1 text-xs text-muted-foreground\">Answering — stop</p>\n          <MessageInput value=\"\" onValueChange={() => {}}>\n            <MessageInputField placeholder=\"Answering…\" disabled />\n            <MessageInputSubmit status=\"streaming\" onStop={() => {}} />\n          </MessageInput>\n        </div>\n      </div>\n    </TooltipProvider>\n  )\n}\n",
      "type": "registry:example",
      "target": "examples/composer/message-input.tsx"
    }
  ],
  "meta": {
    "variants": [
      {
        "name": "Default",
        "demo": "MessageInputDemo"
      },
      {
        "name": "States",
        "demo": "MessageInputStatesDemo"
      }
    ],
    "wide": true
  },
  "categories": [
    "composer"
  ],
  "type": "registry:example"
}